from gendiff.views.json_view import format as json
from gendiff.views.plain_view import format as plain
from gendiff.views.default_view import format as default


FORMATTERS = (DEFAULT, PLAIN, JSON) = ('default', 'plain', 'json')
